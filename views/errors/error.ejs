
<% if (title) { %>
    <div class="title center">
        <h1>
            <%= title %>
        </h1>
        <div>
            <%= message %>
        </div>
    </div>
<% } else { res.redirect('/') } %>

